# ============================================
# Evol-Instruct Pipeline - Dependencies
# ============================================

# --- Core LLM Client ---
openai>=1.0.0                # OpenAI-compatible API client (LM Studio)
google-genai>=2.0.0          # Google Gemini API SDK (mới nhất)

# --- Data Processing ---
datasets>=2.14.0             # HuggingFace Datasets (load vietnamese-legal-documents)
beautifulsoup4>=4.12.0       # Làm sạch content_html → plain text
lxml>=4.9.0                  # HTML/XML parser backend cho BeautifulSoup

# --- Đọc nguồn corpus cục bộ (Phần 3 — corpus_loader) ---
# Chỉ cần khi nguồn dữ liệu về dưới dạng tương ứng; các định dạng khác
# (.jsonl/.json/.csv/.tsv/.txt/.md/.html) dùng thư viện chuẩn, không cần cài gì.
python-docx>=1.1.0           # Đọc .docx (văn bản luật tải từ trang tra cứu)
openpyxl>=3.1.0              # Đọc .xlsx/.xlsm (dataset xuất từ Excel)

# --- NLP & Text Analysis ---
underthesea>=6.8.0           # NER tiếng Việt (Named Entity Recognition)
scikit-learn>=1.3.0          # TF-IDF Vectorizer + Cosine Similarity (bộ lọc)

# --- Resilience & Retry ---
tenacity>=8.2.0              # Retry logic với exponential backoff

# --- Configuration ---
python-dotenv>=1.0.0         # Load biến môi trường từ .env

# --- Logging & Progress ---
loguru>=0.7.0                # Structured logging
tqdm>=4.66.0                 # Progress bars

# --- EDA / Notebook ---
jupyter>=1.0.0               # Jupyter Notebook
notebook>=7.0.0              # Jupyter Notebook server
matplotlib>=3.8.0            # Vẽ đồ thị (EDA figures)
seaborn>=0.13.0              # Statistical visualization
pandas>=2.0.0                # DataFrame & bảng thống kê
numpy>=1.26.0                # Numerical computing
wordcloud>=1.9.0             # Word Cloud (tùy chọn)
ipykernel>=6.0.0             # Kernel Jupyter cho venv

# --- Testing ---
pytest>=7.4.0                # Unit testing framework
pytest-cov>=4.1.0            # Coverage reporting

# --- Utilities ---
jsonlines>=4.0.0             # Đọc/ghi JSONL files
